import { Meta, Story, Canvas } from '@storybook/addon-docs';
import * as NexFooterStories from './NexFooter.stories';

<Meta of={NexFooterStories} />

# NexFooter

A modern, scalable footer component with glassmorphic styling that perfectly complements the NexNav design system. Built with 2025 design trends, clean animations, and enterprise-grade features.

## Features

- **2025 Design Trends**: Modern glassmorphic styling with subtle gradients and clean typography
- **Contact Form Integration**: Built-in contact form for lead generation and customer support
- **Newsletter Signup**: Email subscription with validation and loading states
- **Social Media Integration**: Properly sized social icons with platform-specific styling
- **Agency Attribution**: Built-in agency credit with customizable links and text
- **Multiple Variants**: Default, contact, and black glass layouts
- **Responsive Design**: Mobile-first approach with adaptive grid layouts
- **Accessibility**: WCAG compliant with reduced motion support
- **Theme Support**: Light, dark, and auto theme variants

## Usage

```tsx
import { NexFooter } from '@nexcomponent/ui';

function App() {
  return (
    <NexFooter
      displayName="Your Company"
      tagline="Building amazing products"
      variant="default"
      newsletter={{
        enabled: true,
        placeholder: "Stay updated",
        onSubmit: async (email) => {
          // Handle newsletter subscription
        }
      }}
      contact={{
        enabled: true,
        title: "Get in Touch",
        description: "We'd love to hear from you",
        onSubmit: async (data) => {
          // Handle contact form submission
        }
      }}
      socials={[
        { type: 'github', url: 'https://github.com/yourcompany' },
        { type: 'twitter', url: 'https://twitter.com/yourcompany' }
      ]}
    />
  );
}
```

## Variants

### Default
The standard footer with all features enabled.

<Canvas of={NexFooterStories.Default} />

### Contact
Contact-focused variant with prominent contact form for lead generation.

<Canvas of={NexFooterStories.Contact} />

### Black Glass
Premium black glassmorphic variant for dark themes.

<Canvas of={NexFooterStories.BlackGlass} />

### Minimal
Clean, minimal footer with just branding and social links.

<Canvas of={NexFooterStories.Minimal} />

### Enterprise
Comprehensive enterprise footer with multiple sections and features.

<Canvas of={NexFooterStories.Enterprise} />

### Sections Only
Footer with only navigation sections, no branding.

<Canvas of={NexFooterStories.SectionsOnly} />

## Agency Attribution

The NexFooter component includes a built-in agency attribution feature that allows you to credit your development agency or studio. This feature supports both clickable links and plain text display.

### Agency Attribution Examples

#### Basic Agency Attribution
<Canvas of={NexFooterStories.WithAgencyAttribution} />

#### Black Glass Theme with Agency Attribution
<Canvas of={NexFooterStories.AgencyAttributionBlackGlass} />

#### Agency Attribution Without Link
<Canvas of={NexFooterStories.AgencyAttributionNoLink} />

#### Custom Agency Attribution Text
<Canvas of={NexFooterStories.AgencyAttributionWithCustomTranslation} />

### Agency Attribution Configuration

The `agencyAttribution` prop accepts the following configuration:

```tsx
interface AgencyAttribution {
  /** Website URL for the agency link */
  websiteUrl?: string;
  /** Whether to open the link in a new tab */
  openInNewTab?: boolean;
}
```

#### Configuration Options

- **`websiteUrl`** (optional): The URL that the agency attribution text will link to
  - If provided: The attribution becomes clickable with hover effects
  - If not provided: The attribution displays as plain text
  - Example: `'https://inkblotstudio.com'`

- **`openInNewTab`** (optional): Controls link behavior
  - `true` (default): Opens the link in a new tab/window
  - `false`: Opens the link in the same tab
  - Only applies when `websiteUrl` is provided

#### Visual Appearance

The agency attribution automatically adapts to the footer theme:

- **Default Theme**: Semi-transparent white background with dark text
- **Black Glass Theme**: Semi-transparent black background with light text
- **Hover Effects**: Smooth opacity transitions when clickable
- **Animations**: Fades in with a delay for smooth entrance

#### Customizing Agency Attribution Text

You can customize the agency attribution text using the `translations` prop:

```tsx
<NexFooter
  displayName="MyApp"
  translations={{
    madeByInkblotStudio: 'Crafted with passion by Inkblot Studio'
  }}
  agencyAttribution={{
    websiteUrl: 'https://inkblotstudio.com',
    openInNewTab: true
  }}
/>
```

## Props



## Design System Integration

The NexFooter component is built to seamlessly integrate with the NexNav design system:

- **Consistent Styling**: Matches the navbar's glassmorphic design language
- **Animation Patterns**: Uses the same hover animations and transitions
- **Color Tokens**: Leverages the same design tokens for consistency
- **Typography**: Follows the established font hierarchy and spacing
- **Responsive Behavior**: Adapts to the same breakpoints and layouts

## Accessibility

- **Keyboard Navigation**: Full keyboard support with focus indicators
- **Screen Readers**: Proper ARIA labels and semantic HTML structure
- **Reduced Motion**: Respects user's motion preferences
- **Color Contrast**: Meets WCAG AA standards for text contrast
- **Form Validation**: Accessible form inputs with proper error handling

## Performance

- **Optimized Animations**: Uses CSS transforms and opacity for smooth performance
- **Lazy Loading**: Social icons and images load efficiently
- **Minimal Re-renders**: Optimized React component with proper memoization
- **Bundle Size**: Lightweight with tree-shaking support

## Customization

### Styling
The footer uses CSS custom properties for easy theming:

```scss
.nex-footer {
  --nex-footer-background: rgba(255, 255, 255, 0.06);
  --nex-footer-border: rgba(255, 255, 255, 0.08);
  --nex-footer-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
}
```

### Social Icons
Add custom social platforms by extending the icon mapping:

```tsx
const customSocialIcons = {
  ...socialIcons,
  custom: () => <YourCustomIcon />
};
```

### Contact Form
Customize the contact form behavior and styling:

```tsx
contact={{
  enabled: true,
  title: "Custom Title",
  description: "Custom description",
  placeholder: "Custom placeholder",
  buttonText: "Custom Button",
  onSubmit: async (data) => {
    // Custom submission logic
  }
}}
```

## Best Practices

1. **Content Organization**: Group related links in logical sections
2. **Social Media**: Include only active social platforms
3. **Contact Information**: Provide clear ways for users to reach you
4. **Newsletter**: Use compelling copy to encourage signups
5. **Developer Tools**: Include relevant npm packages and GitHub links
6. **Accessibility**: Test with screen readers and keyboard navigation
7. **Performance**: Monitor bundle size and loading performance
8. **Analytics**: Track newsletter signups and contact form submissions

## Migration Guide

### From v1.x to v2.x

- `variant` prop now supports `'contact'` option
- `contact` prop added for contact form functionality
- Social icons now have fixed sizing (16px default)
- Improved hover animations matching navbar patterns
- Enhanced glassmorphic styling with 2025 design trends

```tsx
// Old
<NexFooter variant="compact" />

// New
<NexFooter 
  variant="contact"
  contact={{
    enabled: true,
    onSubmit: handleContact
  }}
/>
```

## Examples

### Basic Implementation
```tsx
<NexFooter
  displayName="MyApp"
  tagline="Building the future"
  socials={[
    { type: 'github', url: 'https://github.com/myapp' }
  ]}
/>
```

### With Newsletter
```tsx
<NexFooter
  displayName="MyApp"
  newsletter={{
    enabled: true,
    placeholder: "Subscribe for updates",
    onSubmit: handleNewsletter
  }}
/>
```

### Contact Form
```tsx
<NexFooter
  displayName="MyApp"
  variant="contact"
  contact={{
    enabled: true,
    title: "Get in Touch",
    description: "We'd love to hear from you",
    onSubmit: handleContact
  }}
/>
```

### Enterprise Setup
```tsx
<NexFooter
  displayName="MyApp Enterprise"
  logoSrc="/logo.svg"
  sections={[
    {
      title: "Product",
      links: [
        { label: "Features", url: "/features" },
        { label: "Pricing", url: "/pricing" }
      ]
    }
  ]}
  newsletter={{ enabled: true, onSubmit: handleNewsletter }}
  socials={[
    { type: 'github', url: 'https://github.com/myapp' },
    { type: 'linkedin', url: 'https://linkedin.com/company/myapp' }
  ]}
/>
```

### With Agency Attribution
```tsx
<NexFooter
  displayName="MyApp"
  tagline="Building amazing experiences"
  agencyAttribution={{
    websiteUrl: 'https://inkblotstudio.com',
    openInNewTab: true
  }}
  // ... other props
/>
```

### Agency Attribution with Custom Text
```tsx
<NexFooter
  displayName="MyApp"
  translations={{
    madeByInkblotStudio: 'Crafted with passion by Inkblot Studio'
  }}
  agencyAttribution={{
    websiteUrl: 'https://inkblotstudio.com',
    openInNewTab: true
  }}
  // ... other props
/>
```